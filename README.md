# 🌗 Dark & Light Theme Toggle

## 📌 Project Description

This project implements a **Dark and Light Theme Toggle** using **HTML, CSS, and JavaScript**. Users can switch between themes, and their preference is saved using **LocalStorage**, so the selected theme persists after page refresh.

## 🛠️ Technologies Used

* HTML
* CSS (Custom Properties / Variables)
* JavaScript (DOM & LocalStorage)

---
## ⚙️ How It Works

* CSS variables define theme colors
* JavaScript toggles the `data-theme` attribute
* LocalStorage stores user preference
* Saved theme loads automatically on reload

---

## ✅ Output

Users can click the toggle button to switch between Dark and Light modes.

# toggletheme
